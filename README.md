# MVC Clash Demo for node.js hamburg

## Requirements:
- node.js
- couchdb

## Install

```
$ git clone git@github.com:revington/mvc_clash.git
$ cd mvc_clash
$ npm install
```
# Run

`$ ./start` will create the `mvclash` database in your local couchdb and 
run the website on port 3000
